# Basic Cloud Build configuration for Node.js application

steps:
- name: 'node:16'  # Use a specific Node.js version image
  args: ['npm', 'install']  # Install dependencies
- name: 'node:16'
  args: ['npm', 'run', 'test']  # Run tests (assuming a test script in package.json)

